# API base URL used by the production frontend.
# Include the backend API prefix. Example: https://api.example.com/api/v1
VITE_API_URL=/api

# For a separately deployed backend, use its HTTPS URL instead:
# VITE_API_URL=https://your-backend.example.com/api/v1